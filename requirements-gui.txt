# Requirements for building standalone GUI executable
# Use this for: python -m pip install -r requirements-gui.txt

# Core dependencies (same as Docker)
TotalSegmentator>=2.0.0
SimpleITK>=2.2.0
nibabel>=5.0.0
numpy>=1.24.0
pandas>=2.0.0

# GUI + executable building
pyinstaller>=5.0.0
